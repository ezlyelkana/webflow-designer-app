export interface ModalMethods {
    onCompleted?: (x: any) => any;
    onError?: (x: {
        code: string;
        message: string;
    }) => any;
}
export interface ForgotModalProps {
    type: "FORGOT_PASSWORD";
}
export interface LoginModalProps {
    type: "LOGIN";
    planId?: string;
    priceId?: string;
}
export interface SignupModalProps {
    type: "SIGNUP";
    planId?: string;
    priceId?: string;
}
export interface ProfileModalProps {
    type: "PROFILE";
}
export declare const useMemberstackModal: () => {
    openModal: (openModalProps?: (ForgotModalProps | LoginModalProps | SignupModalProps | ProfileModalProps) & ModalMethods) => Promise<unknown>;
    hideModal: () => void;
};
