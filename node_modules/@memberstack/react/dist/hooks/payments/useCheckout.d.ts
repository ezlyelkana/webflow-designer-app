export declare type PurchasePlansWithCheckoutParams = {
    priceId: string;
    cancelUrl?: string;
    successUrl?: string;
    autoRedirect?: boolean;
};
export declare const useCheckout: () => (params: PurchasePlansWithCheckoutParams) => Promise<{
    data: {
        url: string;
    };
}>;
