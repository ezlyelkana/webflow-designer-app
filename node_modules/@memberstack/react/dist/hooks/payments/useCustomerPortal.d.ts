export declare type LaunchStripeCustomerPortalParams = {
    priceIds: string[];
    returnUrl?: string;
    autoRedirect?: boolean;
};
export declare const useCustomerPortal: (params: LaunchStripeCustomerPortalParams) => () => Promise<{
    data: {
        url: string;
    };
}>;
